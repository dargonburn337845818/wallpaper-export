# Wallpaper Export · Agent Guide

- Entry: `export_we_wallpaper.py`
- Windows-only utility; no web/network runtime.
- Uses Wallpaper Engine CLI integration + ffmpeg NVENC.
- Keep the tool self-contained; do not add dependencies to other projects.
- `v*` tags trigger a GitHub Actions release with `WallpaperExport.exe`.
