# Contributing to Wallpaper Export

感谢你愿意改进这个项目。它是一个 Windows-only 的本地工具，改动应保持自包含。

## 开发环境

- Windows
- Python 3.11+
- ffmpeg（含 `h264_nvenc`）

## 提交前

1. 保持工具自包含，不引入跨项目依赖。
2. 不加入网络/遥测/分析逻辑。
3. 不提交个人本地路径、账号信息或录制内容。
4. 如果改动命令行参数，请同步更新 README 中的参数表。

## PR 流程

1. 先搜索已有 Issue，避免重复。
2. 用分支提交，不要直接推 `main`。
3. 说明改动动机、验证方式、影响范围。

## 隐私

本项目不访问网络、不上传数据。若你的改动涉及文件读取或外部命令，请确认不会意外泄漏用户本地路径。
